# Point this at your deployed backend once it's live, e.g.:
# VITE_API_URL=https://wishly-backend.onrender.com
# Leave unset for local development (Vite proxy handles it, see vite.config.js).
VITE_API_URL=
